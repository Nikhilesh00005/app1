# Docker Jenkins CI Pipeline

This project demonstrates a Jenkins CI pipeline that:

1. Checks out source code from Git
2. Builds the Python application
3. Runs automated tests
4. Builds a Docker image
5. Runs the Docker container
6. Performs an application health check

Application port:

5000

Docker host port:

8081
